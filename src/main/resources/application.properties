spring.application.name=demo
spring.datasource.url=jdbc:postgresql://localhost:5432/student
spring.datasource.username=postgres
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

server.error.include-message=always


# Sets the application name to "demo" (used for logging or identification).

# Database connection URL (PostgreSQL on localhost:5432, database named "student")

# Username for connecting to the database

# Password for connecting to the database

# JPA configuration for database schema management

# Enable printing generated SQL statements to the console (for debugging)e

# Set Hibernate dialect for PostgreSQL compatibility

# Format generated SQL for better readability (when show_sql is enabled)

# Include full error messages in error responses (for development/testing)